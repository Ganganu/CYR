<UserControl
  x:Class="CYR.View.ClientView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:CYR.View"
  xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:vm="clr-namespace:CYR.ViewModel"
  d:DataContext="{d:DesignInstance Type=vm:ClientViewModel,
                                   IsDesignTimeCreatable=False}"
  d:DesignHeight="450"
  d:DesignWidth="800"
  mc:Ignorable="d">
  <Grid Background="White">
    <Grid.RowDefinitions>
      <RowDefinition Height="auto" />
      <RowDefinition Height="auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <StackPanel Grid.Row="0">
      <Label
        HorizontalAlignment="Center"
        Content="Kunden"
        FontSize="35" />
      <Border
        Margin="5"
        BorderBrush="LightGray"
        BorderThickness="2" />
    </StackPanel>
    <Grid Grid.Row="1">
      <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
        <Button Command="{Binding UpdateClientCommand}" Style="{StaticResource MaterialDesignFlatButton}">
          <Button.Content>
            <materialDesign:PackIcon Kind="Edit" />
          </Button.Content>
        </Button>
        <Button Command="{Binding DeleteClientCommand}" Style="{StaticResource MaterialDesignFlatButton}">
          <Button.Content>
            <materialDesign:PackIcon Foreground="Red" Kind="Delete" />
          </Button.Content>
        </Button>

      </StackPanel>
    </Grid>
    <DataGrid
      Grid.Row="2"
      AutoGenerateColumns="False"
      CanUserAddRows="False"
      CanUserReorderColumns="False"
      IsReadOnly="True"
      ItemsSource="{Binding Clients}">
      <DataGrid.Columns>
        <DataGridTemplateColumn>
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
              <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
        <DataGridTextColumn Binding="{Binding ClientNumber}" Header="Kundennummer" />
        <DataGridTextColumn Binding="{Binding Name}" Header="Name" />
        <DataGridTextColumn Binding="{Binding Street}" Header="Straße" />
        <DataGridTextColumn Binding="{Binding City}" Header="Stadt" />
        <DataGridTextColumn Binding="{Binding PLZ}" Header="PLZ" />
        <DataGridTextColumn Binding="{Binding EmailAddress}" Header="Email-Adresse" />
        <DataGridTextColumn Binding="{Binding Telefonnumber}" Header="Telefonnummer" />
      </DataGrid.Columns>
    </DataGrid>
    <Button
      Grid.Row="2"
      Margin="0,0,20,30"
      HorizontalAlignment="Right"
      VerticalAlignment="Bottom"
      Command="{Binding CreateNewClientCommand}"
      Style="{StaticResource MaterialDesignFloatingActionButton}">
      <Button.Content>
        <materialDesign:PackIcon
          Width="30"
          Height="30"
          Kind="UserAdd" />
      </Button.Content>
    </Button>
  </Grid>
</UserControl>
