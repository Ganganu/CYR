<UserControl
  x:Class="CYR.View.ClientView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:CYR.View"
  xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:vm="clr-namespace:CYR.ViewModel"
  d:DataContext="{d:DesignInstance Type=vm:ClientViewModel,
                                   IsDesignTimeCreatable=False}"
  d:DesignHeight="450"
  d:DesignWidth="800"
  mc:Ignorable="d">
  <Grid Background="White">
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <FrameworkElement x:Name="ProxyElement" Visibility="Collapsed" />
    <DataGrid
      Grid.Row="0"
      AutoGenerateColumns="False"
      CanUserAddRows="False"
      CanUserReorderColumns="False"
      IsReadOnly="True"
      ItemsSource="{Binding Clients}">
      <DataGrid.Columns>
        <DataGridTextColumn Binding="{Binding ClientNumber}" Header="Kundennummer" />
        <DataGridTextColumn Binding="{Binding Name}" Header="Name" />
        <DataGridTextColumn Binding="{Binding Street}" Header="Straße" />
        <DataGridTextColumn Binding="{Binding City}" Header="Stadt" />
        <DataGridTextColumn Binding="{Binding PLZ}" Header="PLZ" />
        <DataGridTextColumn Binding="{Binding EmailAddress}" Header="Email-Adresse" />
        <DataGridTextColumn Binding="{Binding Telefonnumber}" Header="Telefonnummer" />
      </DataGrid.Columns>
    </DataGrid>
    <Button
      Margin="0,0,20,30"
      HorizontalAlignment="Right"
      VerticalAlignment="Bottom"
      Command="{Binding CreateNewClientCommand}"
      Style="{StaticResource MaterialDesignFloatingActionButton}">
      <Button.Content>
        <materialDesign:PackIcon
          Width="30"
          Height="30"
          Kind="UserAdd" />
      </Button.Content>
    </Button>
  </Grid>
</UserControl>
