<UserControl
  x:Class="CYR.View.InvoiceView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:CYR.View"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:vm="clr-namespace:CYR.ViewModel"
  d:DataContext="{d:DesignInstance Type=vm:InvoiceViewModel,
                                   IsDesignTimeCreatable=False}"
  d:DesignHeight="450"
  d:DesignWidth="800"
  mc:Ignorable="d">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <Button
      Grid.Column="0"
      Height="70"
      Margin="10"
      Command="{Binding NavigateToCreateNewInvoiceCommand}"
      Content="Neue Rechnung schreiben"
      FontSize="15" />
    <Button
      Grid.Column="1"
      Height="70"
      Margin="10"
      Command="{Binding NavigateToGetInvoicesCommand}"
      Content="Rechnungen durchsuchen"
      FontSize="15" />
  </Grid>
</UserControl>
