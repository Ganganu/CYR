<UserControl x:Class="CYR.View.InvoiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CYR.View"
             mc:Ignorable="d"
             xmlns:vm="clr-namespace:CYR.ViewModel"
             d:DataContext="{d:DesignInstance Type=vm:InvoiceViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" FontSize="15" Margin="10" Height="70" Content="Neue Rechnung schreiben" Command="{Binding NavigateToCreateNewInvoiceCommand}"/>
        <Button Grid.Column="1" FontSize="15" Margin="10" Height="70" Content="Rechnungen durchsuchen" Command="{Binding NavigateToGetInvoicesCommand}"/>
    </Grid>
</UserControl>
