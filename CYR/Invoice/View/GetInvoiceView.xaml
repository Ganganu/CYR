<UserControl x:Class="CYR.View.GetInvoiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CYR.View"
             xmlns:vm="clr-namespace:CYR.ViewModel"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:GetInvoiceViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid ItemsSource="{Binding Invoices}"
                  CanUserAddRows="False"
                  IsReadOnly="True" AutoGenerateColumns="False" x:Name="dgInvoices">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Rechnungsnummer" Binding="{Binding InvoiceNumber}"/>
                <DataGridTextColumn Header="Kundenummer" Binding="{Binding Customer.ClientNumber}"/>
                <DataGridTextColumn Header="Kundenname" Binding="{Binding Customer.Name}"/>
                <DataGridTextColumn Header="Betreff" Binding="{Binding Subject}"/>
                <DataGridTextColumn Header="Objektnummer" Binding="{Binding ObjectNumber}"/>
                <DataGridTextColumn Header="Netto" Binding="{Binding NetAmount}"/>
                <DataGridTextColumn Header="Brutto" Binding="{Binding GrossAmount}"/>
            </DataGrid.Columns>
            <DataGrid.InputBindings>
                <MouseBinding Command="{Binding SelectInvoiceCommand}"
                              CommandParameter="{Binding SelectedItem, ElementName=dgInvoices}"
                              MouseAction="LeftDoubleClick"/>
            </DataGrid.InputBindings>
        </DataGrid>
    </Grid>
</UserControl>
