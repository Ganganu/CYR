<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:converters="clr-namespace:CYR.Invoice.Resources.DataTemplates.Converters"
  xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

  <converters:StateToBackgroundConverter x:Key="StateToBackgroundConverter" />
  <converters:StateToBorderBrushConverter x:Key="StateToBorderBrushConverter" />
  <converters:StateToStringConverter x:Key="StateToStringConverter" />

  <DataTemplate x:Key="OpenTemplate">
    <Border
      Background="{Binding State, Converter={StaticResource StateToBackgroundConverter}}"
      BorderBrush="{Binding State, Converter={StaticResource StateToBorderBrushConverter}}"
      BorderThickness=".5"
      CornerRadius="5">
      <Label Content="{Binding State, Converter={StaticResource StateToStringConverter}}" Foreground="{Binding State, Converter={StaticResource StateToBorderBrushConverter}}" />
    </Border>
  </DataTemplate>

  <DataTemplate x:Key="ClosedTemplate">
    <Border
      Background="{Binding State, Converter={StaticResource StateToBackgroundConverter}}"
      BorderBrush="{Binding State, Converter={StaticResource StateToBorderBrushConverter}}"
      BorderThickness=".5"
      CornerRadius="5">
      <Label Content="{Binding State, Converter={StaticResource StateToStringConverter}}" Foreground="{Binding State, Converter={StaticResource StateToBorderBrushConverter}}" />
    </Border>
  </DataTemplate>


  <Style BasedOn="{StaticResource {x:Type DataGridRow}}" TargetType="DataGridRow">
    <Style.Triggers>
      <DataTrigger Binding="{Binding IsSelected}" Value="True">
        <Setter Property="Background" Value="#FFEFD7" />
      </DataTrigger>
    </Style.Triggers>
  </Style>
  <Style TargetType="DataGridCell">
    <Setter Property="Padding" Value="11" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderBrush" Value="Transparent" />
    <Setter Property="Validation.ErrorTemplate" Value="{x:Null}" />
    <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=DataGridRow}}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type DataGridCell}">
          <Grid>
            <Border
              Background="{TemplateBinding Background}"
              BorderBrush="{TemplateBinding BorderBrush}"
              BorderThickness="{TemplateBinding BorderThickness}"
              SnapsToDevicePixels="True" />
            <ContentPresenter Margin="{TemplateBinding Padding}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>