<UserControl
  x:Class="CYR.Invoice.View.InvoiceActionsView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:CYR.Invoice.View"
  xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:vm="clr-namespace:CYR.Invoice.ViewModel"
  d:DataContext="{d:DesignInstance Type=vm:InvoiceActionsViewModel,
                                   IsDesignTimeCreatable=False}"
  d:DesignHeight="450"
  d:DesignWidth="800"
  mc:Ignorable="d">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="3*" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <ComboBox Grid.Column="0" ItemsSource="{Binding InvoiceState}" />
    <Button Grid.Column="1" Style="{StaticResource MaterialDesignFlatButton}">
      <Button.Content>
        <materialDesign:PackIcon Kind="DotsHorizontal" />
      </Button.Content>
    </Button>
  </Grid>
</UserControl>
