<UserControl
  x:Class="CYR.Invoice.InvoiceViews.ShowInvoiceView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:CYR.View"
  xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:vm="clr-namespace:CYR.ViewModel"
  d:Background="White"
  d:DataContext="{d:DesignInstance Type=vm:ShowInvoiceViewModel,
                                   IsDesignTimeCreatable=False}"
  d:DesignHeight="450"
  d:DesignWidth="800"
  FontSize="18"
  mc:Ignorable="d">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="auto" />
      <RowDefinition Height="auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Grid Grid.Row="0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="auto" />
        <RowDefinition Height="*" />
        <RowDefinition Height="*" />
        <RowDefinition Height="*" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>
      <Button
        Grid.Row="0"
        Margin="0,10,0,10"
        HorizontalAlignment="Left"
        Command="{Binding NavigateBackCommand}"
        Style="{StaticResource MaterialDesignFlatButton}">
        <Button.Content>
          <materialDesign:PackIcon Kind="ArrowLeft" />
        </Button.Content>
      </Button>
      <TextBlock Grid.Row="1" Text="{Binding ClientName}" />
      <TextBlock Grid.Row="2" Text="{Binding ClientStreet}" />
      <StackPanel Grid.Row="3" Orientation="Horizontal">
        <TextBlock Text="{Binding ClientZip}" />
        <TextBlock Text=" " />
        <TextBlock Text="{Binding ClientCity}" />
      </StackPanel>
    </Grid>
    <Grid Grid.Row="1">
      <Grid.RowDefinitions>
        <RowDefinition Height="auto" />
        <RowDefinition Height="auto" />
        <RowDefinition Height="auto" />
      </Grid.RowDefinitions>
      <TextBlock Grid.Row="0" Text="{Binding Subject}" />
      <TextBlock Grid.Row="1" Text="{Binding ObjectNumber}" />
      <StackPanel Grid.Row="2" Orientation="Horizontal">
        <TextBlock Text="Zeitraum: " />
        <TextBlock Text="{Binding StartDate}" />
        <TextBlock Text="-" />
        <TextBlock Text="{Binding EndDate}" />
      </StackPanel>
    </Grid>
    <Grid Grid.Row="2">
      <DataGrid
        AutoGenerateColumns="False"
        Background="White"
        CanUserAddRows="False"
        ItemsSource="{Binding Items}">
        <DataGrid.Columns>
          <DataGridTextColumn Binding="{Binding Quantity}" Header="Menge" />
          <DataGridTextColumn Binding="{Binding UnitOfMeasure}" Header="Einheit" />
          <DataGridTextColumn Binding="{Binding Description}" Header="Beschreibung" />
          <DataGridTextColumn Binding="{Binding UnitPrice}" Header="Nettopreis" />
          <DataGridTextColumn Binding="{Binding TotalPrice}" Header="Gesamtpreis" />
        </DataGrid.Columns>
      </DataGrid>
    </Grid>
  </Grid>
</UserControl>
