<UserControl
  x:Class="CYR.Dashboard.DashboardViews.DashboardUserView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:CYR.Dashboard.DashboardViews"
  xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:vm="clr-namespace:CYR.Dashboard.DashboardViewModels"
  d:DataContext="{d:DesignInstance Type=vm:DashboardUserViewModel,
                                   IsDesignTimeCreatable=False}"
  d:DesignHeight="450"
  d:DesignWidth="800"
  mc:Ignorable="d">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Grid Grid.Row="0">
      <Grid.RowDefinitions>
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>
      <Ellipse
        Width="150"
        Height="150"
        Stroke="LightGray"
        StrokeThickness="3">
        <Ellipse.Fill>
          <ImageBrush ImageSource="{Binding UserLogo, UpdateSourceTrigger=PropertyChanged}" Stretch="Uniform" />
        </Ellipse.Fill>
      </Ellipse>
    </Grid>
    <Grid
      Grid.Row="1"
      Margin="0,0,0,0"
      VerticalAlignment="Stretch">
      <Grid.RowDefinitions>
        <RowDefinition Height="*" />
        <RowDefinition Height="auto" />
      </Grid.RowDefinitions>
      <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel
          Grid.Row="0"
          Margin="0,0,0,0"
          Background="#f5f5f5">
          <TextBlock
            Margin="10,0,10,0"
            FontSize="15"
            Text="Benutzername" />
          <TextBox
            Margin="10,0,10,0"
            FontSize="15"
            IsReadOnly="True"
            Style="{StaticResource MaterialDesignOutlinedTextBox}"
            Text="{Binding UserCompany.Username}" />
          <TextBlock
            Margin="10,0,10,0"
            FontSize="15"
            Text="Role" />
          <TextBox
            Margin="10,0,10,0"
            FontSize="15"
            IsReadOnly="True"
            Style="{StaticResource MaterialDesignOutlinedTextBox}"
            Text="{Binding UserCompany.Role}" />
          <TextBlock
            Margin="10,0,10,0"
            FontSize="15"
            Text="Firmenname" />
          <TextBox
            Margin="10,0,10,0"
            FontSize="15"
            IsReadOnly="True"
            Style="{StaticResource MaterialDesignOutlinedTextBox}"
            Text="{Binding UserCompany.CompanyName}" />
          <TextBlock
            Margin="10,0,10,0"
            FontSize="15"
            Text="Email-Adresse" />
          <TextBox
            Margin="10,0,10,0"
            FontSize="15"
            IsReadOnly="True"
            Style="{StaticResource MaterialDesignOutlinedTextBox}"
            Text="{Binding UserCompany.CompanyEmailAddress}" />
          <TextBlock
            Margin="10,0,10,0"
            FontSize="15"
            Text="Telefonnummer" />
          <TextBox
            Margin="10,0,10,0"
            FontSize="15"
            IsReadOnly="True"
            Style="{StaticResource MaterialDesignOutlinedTextBox}"
            Text="{Binding UserCompany.CompanyTelefonNumber}" />
        </StackPanel>
      </ScrollViewer>
      <Grid
        Grid.Row="1"
        Margin="10"
        VerticalAlignment="Bottom"
        Background="#f5f5f5"
        Cursor="">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Button
          Grid.Column="0"
          Command="{Binding NavigateToUserViewCommand}"
          Content="Konto Aktualisieren" />
      </Grid>
    </Grid>
  </Grid>
</UserControl>
